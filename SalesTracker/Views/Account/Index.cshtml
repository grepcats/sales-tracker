@model IEnumerable<Microsoft.AspNetCore.Identity.EntityFrameworkCore.IdentityRole>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>This is the index page for Account</h2>

@*@using System.Security.Claims


<h2>Roles Listing</h2>
@Html.ActionLink("Create new role", "Create") | @Html.ActionLink("Manage User Role", "ManageUserRoles")
<hr/>

<div>
    @foreach(var role in Model)
    {
    <p><strong>@role.Name | </strong> <span onclick="return confirm('Are you sure to delete?')"><a href="/Account/Delete?RoleName=@role.Name" class="delLink" style="color:red;">Delete</a></span> | @Html.ActionLink("Edit", "Edit", new { roleName = @role.Name })</p>

    }
</div>*@
